<nav class="navbar navbar-inverse navbar-fixed-top">
    <div class="navbar-header">
        <button type="button" data-target="#navbarCollapse" data-toggle="collapse" class="navbar-toggle">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
        </button>
        <a href=<%= root_path %> class="navbar-brand">MTGInventory</a>
    </div>
    <div id="navbarCollapse" class="collapse navbar-collapse">
        <ul class="nav navbar-nav">
            <li class="<%= 'active' if params[:controller] == 'dashboard' %>">
              <%= link_to 'Home', root_path %>
            </li>
            <li class="<%= 'active' if params[:controller] == 'inventory' %>">
              <%= link_to 'Inventory', inventories_path %>
            </li>
            <li class="<%= 'active' if params[:controller] == 'card' %>">
              <%= link_to 'Cards', cards_path %>
            </li>
            <li>
              <%= link_to 'My Profile', profile_path if current_user %>
            </li>
        </ul>

        <%= form_tag('/cards', method: 'get') do %>
          <div class="navbar-form navbar-left">
            <div class="input-group">
              <%= text_field_tag :q, nil, class: 'form-control' %>
              <div class="input-group-btn">
                <%= submit_tag 'Search', method: :get, class: 'btn btn-default' %>
              </div>
            </div>
          </div>
        <% end %>

      <ul class="nav navbar-nav navbar-right" style="padding-right: 1.5em;">
          <li>
            <% if current_user %>
              <%= link_to 'Logout', destroy_user_session_path, method: :delete %>
            <% else %>
              <%= link_to 'Login', new_user_session_path %>
            <% end %>
          </li>
        </ul>
    </div>
</nav>
<div id="divSpacer" style="height: 65px;">&nbsp;</div>
